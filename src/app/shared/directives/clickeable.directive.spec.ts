import { ClickeableDirective } from './clickeable.directive';

describe('ClickeableDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickeableDirective();
    expect(directive).toBeTruthy();
  });
});
